name = "second-brain"
compatibility_date = "2024-04-05"
pages_build_output_dir = ".next"

# D1 Database Binding
[[d1_databases]]
binding = "DB"
database_name = "second-brain-db"
database_id = "f6867778-1ef4-41e3-8b4c-cb0fffa38a02"

# Vectorize Index Binding
[[vectorize]]
binding = "VECTORIZE"
index_name = "second-brain-vectors"

# AI Binding (Optional, if we use Cloudflare AI in future, but we use Ollama for now)
# [ai]
# binding = "AI"

[vars]
# OLLAMA_URL = "ollama.saaaai.com"
